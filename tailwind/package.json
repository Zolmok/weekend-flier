{
  "name": "tailwind",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production npx @tailwindcss/cli -i ./src/tailwind.css -o ../static/css/styles.css --minify",
    "build:dev": "npx @tailwindcss/cli -i ./src/tailwind.css -o ../static/css/styles.css",
    "build:watch": "npx @tailwindcss/cli -i ./src/tailwind.css -o ./static/css/styles.css --watch",
    "postupdates": "npm install && npm audit fix",
    "test": "echo \"Error: no test specified\" && exit 1",
    "updates": "ncu -u"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.6",
    "tailwindcss": "^4.0.6"
  }
}
